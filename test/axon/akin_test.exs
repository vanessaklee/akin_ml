defmodule Axon.Akin.Test do
  use ExUnit.Case
  doctest AkinML

  test "Performs predictions using Axon" do
    # Axon.Akin.perform()
  end

  test "strings match" do
    # input = [[0.43, 0.93, 0.47, 0.0, 0.0, 0.55, 0.31, 0.96, 0.43, 0.32, 0.9, 0.88, 0.31]] |> Nx.tensor()

    # prediction = Axon.Akin.predict(model, input)

    # assert prediction == 1
  end

  test "strings do not match" do

  end
end
